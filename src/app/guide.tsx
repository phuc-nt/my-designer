import { PublicNavigation } from './public-navigation';
import { useEffect, useRef, useState } from 'react';
import { ArrowRight, ArrowUpRight, Boxes, Check, ChevronDown, Copy, MessageSquare, MousePointer2, Sparkles } from 'lucide-react';
import './guide.css';
import { ThemeToggle } from './theme-toggle';

const briefs = {
  Website: 'Create a calm, welcoming landing page for a neighborhood ceramics studio. Help beginners understand the classes and book their first workshop. Use warm ivory, clay accents, large readable type, and generous spacing. Include a short introduction, three class options, and a clear booking button. Start with a desktop layout, then check it at a smaller viewport.',
  Slides: 'Create a six-slide product introduction for a small business audience. Explain the customer problem, our solution, three practical benefits, and a clear next step. Use one idea per slide, concise headlines, consistent typography, and a warm neutral palette. Ask me for missing product facts before inventing them.',
  Report: 'Create a readable quarterly report layout for our team. Include an executive summary, key metrics, progress, challenges, and next steps. Use clear section headings, consistent spacing, and accessible contrast. Keep unknown figures as explicitly labeled fields for me to fill; do not invent performance data.',
  '3D scene': 'Create a product-display scene with a simple editable 3D object and a concise title. Use a neutral background, a clay-colored material, and enough whitespace for a short description. Keep geometry, material, and rotation easy to refine. Ask me for the actual product asset if a precise model is needed.',
  Video: 'Create a short product announcement using a simple timeline. Introduce the idea, show one benefit at a time, then end with a clear next step. Use readable captions, gentle motion, and a consistent palette. Ask for the actual product name and approved copy before finalizing. Keep it under 15 seconds.',
};
type BriefKind = keyof typeof briefs;
const outline = [['start', 'Choose a starting point'], ['brief', 'Describe the outcome'], ['conversation', 'Refine through conversation'], ['preview', 'Make it yours'], ['deliver', 'Export or share'], ['agents', 'Connect an agent']] as const;

function GuideImage({ file, alt, caption }: { file: string; alt: string; caption: string }) { return <figure className="guide-screenshot"><div className="guide-window-bar"><span /><span /><span /><small>Design Studio AI</small></div><img src={`/guide/assets/${file}`} alt={alt} loading="lazy" width="1440" height="1000" /><figcaption>{caption}</figcaption></figure>; }

export function GuideApp() {
  const [ready, setReady] = useState(false);
  useEffect(() => setReady(true), []);
  const [kind, setKind] = useState<BriefKind>('Website');
  const [copied, setCopied] = useState(false);
  const [copyError, setCopyError] = useState(false);
  const timer = useRef<ReturnType<typeof setTimeout> | undefined>(undefined);
  useEffect(() => { const title = document.title; document.title = 'Your first design · Design Studio AI Guide'; return () => { document.title = title; clearTimeout(timer.current); }; }, []);
  async function copyBrief() { clearTimeout(timer.current); setCopyError(false); try { await navigator.clipboard.writeText(briefs[kind]); setCopied(true); timer.current = setTimeout(() => setCopied(false), 4000); } catch { setCopyError(true); } }
  return <div className="guide-app" data-interactive={ready}><a className="guide-skip" href="#guide-content">Skip to the guide</a><PublicNavigation active="guide"/><main id="guide-content"><section className="guide-hero"><span className="guide-eyebrow">A FRIENDLY FIELD GUIDE</span><h1>Your first idea,<br /><em>made visible.</em></h1><p>You bring the intent. Your agent helps with the design.<br className="guide-desktop-break" /> A few thoughtful steps turn the conversation into something you can use.</p><a href="#start" className="guide-primary">Let's make something <ArrowRight size={17} /></a><div className="guide-hero-notes"><span><MessageSquare size={15} />Describe it</span><span><MousePointer2 size={15} />Refine it</span><span><ArrowUpRight size={15} />Share it</span></div><div className="guide-hero-art" aria-hidden="true"><div className="guide-art-sheet"><span>A little more room<br />for a good idea.</span><i /><i /><i /></div><div className="guide-art-note"><Sparkles size={17} /><span>Start with what<br />you want to say.</span></div><div className="guide-art-cursor"><MousePointer2 size={28} /></div></div></section><nav className="guide-outline" aria-label="Guide steps">{outline.map(([id, name], index) => <a key={id} href={`#${id}`}><span>0{index + 1}</span>{name}</a>)}</nav><div className="guide-content"><section id="start" className="guide-section"><div className="guide-step-number">01</div><div className="guide-step-body"><span className="guide-eyebrow">BEGIN WITH A CANVAS</span><h2>Choose a starting point.</h2><p>Open the studio and sign in. Write your idea on the homepage and choose Let’s create to save a project and begin its design interview. You can also start from a template when its structure suits your idea, or choose a blank project. Websites, slides, reports, wireframes, 3D scenes, and videos share the same editable workspace.</p><div className="guide-tip"><strong>No provider key needed to explore.</strong><p>Templates and manual edits work on their own. Add your own AI-provider connection in Settings when you're ready to generate a design or media.</p></div><GuideImage file="home-desktop.png" alt="The Design Studio project library and template choices on a desktop screen" caption="Choose the kind of work you want to make, then open a starter or a blank project." /></div></section><section id="brief" className="guide-section"><div className="guide-step-number">02</div><div className="guide-step-body"><span className="guide-eyebrow">MAKE THE BRIEF USEFUL</span><h2>Describe the outcome,<br />not every pixel.</h2><p>A useful brief explains who this is for, what they should understand or do, what content is real, and how the design should feel. Give the agent your constraints before asking it to fill the canvas.</p><div className="guide-question-grid"><div><span>WHO</span><h3>Who is looking?</h3><p>A first-time visitor, a customer, your team, or an investor?</p></div><div><span>WHAT</span><h3>What should happen?</h3><p>Understand an idea, compare options, book a class, or take a next step?</p></div><div><span>WITHIN</span><h3>What must stay true?</h3><p>Your approved copy, actual data, brand, dimensions, deadline, and assets.</p></div></div><div className="guide-brief"><div className="guide-brief-heading"><span>STARTER BRIEF</span><label>For <select value={kind} onChange={event => { setKind(event.target.value as BriefKind); setCopied(false); setCopyError(false); }} aria-label="Choose a starter brief">{Object.keys(briefs).map(value => <option key={value}>{value}</option>)}</select></label></div><p>{briefs[kind]}</p><div className="guide-brief-footer"><span>Adapt the details to your real project.</span><button type="button" onClick={() => void copyBrief()} disabled={!ready}>{copied ? <Check size={15} /> : <Copy size={15} />}{copied ? 'Copied' : 'Copy starter brief'}</button></div><span role="status" className="guide-copy-status">{copied ? 'Starter brief copied.' : copyError ? 'Clipboard unavailable. Select and copy the brief manually.' : ''}</span></div><p className="guide-small">If you're unsure, ask the agent to interview you first: “Ask me the few questions you need to clarify the audience, scope, content, and success criteria.” The studio saves your brief, questions, answers, and scope so you or a connected agent can continue later.</p></div></section><section id="conversation" className="guide-section"><div className="guide-step-number">03</div><div className="guide-step-body"><span className="guide-eyebrow">WORK THROUGH THE CONVERSATION</span><h2>Agree on the scope.<br />Then make the first draft.</h2><p>Choose Ask AI to start the interview with a configured text provider. Answer the contextual questions, choose Save answers, and Continue interview until the direction is clear. If you prefer another agent, choose Use your own agent; you can also Write scope manually.</p><div className="guide-tip"><strong>Make approval a deliberate step.</strong><p>Review Objective, Audience, Visual direction, Deliverables, Constraints, and Acceptance criteria. Use Save scope, then Approve scope. Editing the brief again requires fresh approval. The separate Generate design action creates and saves the first validated draft; it does not publish.</p></div><p>You can Open editor to work manually while the brief remains unapproved. Reload latest brief picks up changes from an external agent. Once a draft exists, use project chat for focused refinements, then review a proposal before applying and saving it.</p><div className="guide-conversation"><div className="guide-conversation-label">EXAMPLE REFINEMENT REQUESTS</div><div><span>You</span><p>Keep the layout. Make the headline shorter and give the introduction more breathing room.</p></div><div><span>You</span><p>Use the same type scale on every slide. Keep the approved product copy unchanged.</p></div><div><span>You</span><p>Before we add another section, tell me whether it helps the visitor book a class.</p></div></div><p>Ask for focused changes instead of starting over each time. Treat facts, figures, and product claims as your source material; ask the agent to request missing information rather than invent it.</p><div className="guide-tip"><strong>Generation uses your provider account.</strong><p>Text, image, speech, music/effects, and video calls need compatible BYOK connections and may incur charges. Creating or editing media sends your selected source to that provider; it doesn't automatically publish the source.</p></div></div></section><section id="preview" className="guide-section"><div className="guide-step-number">04</div><div className="guide-step-body"><span className="guide-eyebrow">KEEP YOUR HAND ON THE DESIGN</span><h2>Inspect it. Make it yours.</h2><p>Select an object to refine its text, position, dimensions, color, or other supported properties. Use pages and layers to navigate a larger design, switch themes for a coherent starting palette, and save once the result feels right.</p><GuideImage file="editor-desktop.png" alt="The Design Studio editor showing the conversation panel, editable preview, and object inspector" caption="Conversation, preview, and manual controls stay close together so you can inspect a change before saving." /><div className="guide-checklist"><h3>A quick quality pass</h3>{['Can you read every headline and body line at the intended size?', 'Do spacing, alignment, and typography feel consistent?', 'Are real images, data, and approved words preserved?', 'Does the main action stand out without competing messages?', 'Does the design still work at a smaller viewport?', 'For motion, are captions readable and timing intentional?'].map(text => <div key={text}><Check size={15} /><span>{text}</span></div>)}</div><p>The design preflight can flag likely overflow, text fitting, estimated contrast, and missing media. Use these findings to guide inspection; automated checks do not certify accessibility or aesthetics.</p><p className="guide-small">Another person or agent may edit the same project. If saving reports a revision conflict, reload and reconcile the changes. Don't overwrite newer work blindly. In 3D, geometry/material/object rotation persist; orbit-camera changes are temporary preview state.</p></div></section><section id="deliver" className="guide-section"><div className="guide-step-number">05</div><div className="guide-step-body"><span className="guide-eyebrow">MAKE THE OUTPUT USEFUL</span><h2>Export the file.<br />Share the right version.</h2><p>Choose an output for the person who will use it. Open the exported file and check its contents. Save before exporting or publishing so you're delivering the revision you inspected.</p><div className="guide-output-grid"><div><h3>For a visual</h3><p>PNG for a rendered page. SVG for supported static vector structure.</p></div><div><h3>For a document</h3><p>PDF for pages. PowerPoint for editable text/shapes with complex content rasterized.</p></div><div><h3>For the web</h3><p>HTML for a standalone design, including the trusted 3D/timeline viewer where supported.</p></div><div><h3>For motion</h3><p><a href="/docs/motion">Character rigging, clips, skins and native motion packages</a></p><p>WebM for recorded timeline output. MP4 when the runtime has a supported encoder.</p></div></div><div className="guide-publish"><GlobeMark /><div><h3>Publishing makes a public snapshot.</h3><p>It includes referenced snapshot assets. Later private edits don't change that link. Use Unpublish to remove the project's public snapshots.</p></div></div><p className="guide-small">Cloud binary export needs imported project assets for remote media and enforces size limits. Motion is limited to 60 seconds. Cloud recordings mix imported audio/video; browser fallback is silent. Google Slides needs Google authorization and supports native text/shapes/HTTPS images, with explicit errors for unsupported content.</p></div></section><section id="agents" className="guide-section"><div className="guide-step-number">06</div><div className="guide-step-body"><span className="guide-eyebrow">LET YOUR AGENT JOIN THE WORKSPACE</span><h2>The same design,<br />from another tool.</h2><p>Use the studio directly, or connect the agent you already work with. Your agent reads the same project, makes targeted edits, and saves with the same revision protection.</p><div className="guide-connect-grid"><a href="/?settings=agents"><Boxes size={21} /><h3>API key & CLI</h3><p>Create a named key, inject it securely, and use the released dsa CLI.</p><span>Manage access <ArrowUpRight size={14} /></span></a><a href="/docs/mcp"><MessageSquare size={21} /><h3>Remote MCP</h3><p>Connect through Streamable HTTP with an API key or OAuth consent.</p><span>Connection guide <ArrowRight size={14} /></span></a><a href="/docs/webmcp"><MousePointer2 size={21} /><h3>Browser WebMCP</h3><p>In supported browsers, let an agent edit the open canvas and save explicitly.</p><span>Browser tools <ArrowRight size={14} /></span></a></div></div></section><section className="guide-faq"><h2>Share something useful.</h2><p>Publish a reviewed design to <a href="/community">Community</a>, explore by category, or download a design package to make your own. Your private work stays private until you confirm the public version and license. <a href="/docs/community">Learn about publishing and remixing</a>.</p></section><section className="guide-faq"><span className="guide-eyebrow">A FEW THINGS YOU MIGHT WONDER</span><h2>Before you begin.</h2>{[['Do I need to know how to code?', 'No. You can create from a template, describe changes in chat, and use the manual editor. The CLI and APIs are available when you want to connect another agent or automate a workflow.'], ['Is an AI-provider key included?', 'No. You bring your own provider credentials and model access. Template selection and manual editing work without a provider. Generation calls use your account and may incur charges.'], ['Will the agent automatically publish my work?', 'Generating a proposal, saving a project, and publishing a public snapshot are separate operations. Tell your agent what sharing is authorized, and inspect the saved version before publication.'], ['Can I edit a generated image or video?', 'Compatible OpenAI/fal modes accept a source asset owned by the project. Image, video, and audio transformations have provider-specific requirements. Check the provider guide before choosing a model or source.'], ['Why does an exported file look different?', 'Different formats preserve different structures. SVG is static, PowerPoint rasterizes complex nodes, and Google Slides supports a smaller set of native elements. Always inspect the actual file; export support does not mean full editable parity.']].map(([question, answer]) => <details key={question}><summary>{question}<ChevronDown size={16} /></summary><p>{answer}</p></details>)}</section><section className="guide-closing"><Sparkles size={22} /><h2>Start with one clear idea.</h2><p>You can refine the details together.</p><a className="guide-primary" href="/">Open Design Studio <ArrowUpRight size={17} /></a><a href="/docs">Or explore the documentation <ArrowRight size={14} /></a></section></div></main><footer className="guide-footer"><a href="/">Design Studio AI</a><span>Open source · MIT licensed</span><a href="/docs">Documentation <ArrowUpRight size={14} /></a></footer></div>;
}
function GlobeMark() { return <ArrowUpRight size={25} />; }
export { briefs as guideBriefs, outline as guideOutline };
export default GuideApp;

export function SceneDocumentation(){return <>
  <h2>Build an editable 3D character</h2><p>Open a 3D project and choose Character authoring. Human controls and agent tools use the same validated geometry, skeleton and animation data.</p>
  <ol><li><strong>Shape.</strong> Remesh unparented sphere primitives or closed document meshes into a continuous surface. Preview first; applying keeps source objects hidden. Use joint-ring lofts for controlled limb topology. Imported asset nodes need editable document geometry first.</li><li><strong>Rig.</strong> Create a quadruped or winged quadruped skeleton from explicit landmarks. For wings, mark shoulder, elbow, wrist and 3–5 finger base/tip pairs on each side, plus jaw and jaw tip. The tool does not infer anatomy. Auto-bind the continuous mesh, inspect the heatmap and smooth or mirror weights. Attach eyes and accessories to a named bone.</li><li><strong>Animate.</strong> Pose with FK or bounded IK, then add idle, wag, walk, wing-flap or roar clips. Wing-flap includes adjustable wrist lag; roar animates the skeleton and needs a separate sound cue. Presets reject missing native bones rather than retargeting arbitrary imported rigs. Inspect contact and deformation across the timeline. Morph targets add editable expressions.</li><li><strong>Finish.</strong> Cut and pack UV islands, paint layered color, normal and roughness maps at 256–2048px and inspect topology/weights. Export GLB/glTF and reopen the file to verify meshes, skeletons, morph targets, textures and clips.</li></ol>
  <h2>Import a rigged model</h2><p>Upload a GLB in Assets, then explicitly insert it. Upload only adds the file to the library. <strong>Imported model &amp; animation</strong> shows mesh, bone, morph and clip inventory. Add an observed clip name and set start/end, speed, weight and loop; placements persist in <code>scene.importedClips</code> and use the same sampled playback in the editor and exports.</p>
  <p><strong>Preview editable conversion</strong> reports what can become native meshes, PBR maps, skinning, position morphs and sampled animation; <strong>Apply</strong> retains the original GLB asset in a hidden, locked source checkpoint. Conversion rejects nonunit rest or animated bone scale, unsupported physical material extensions such as clearcoat/transmission, authored morph normals/tangents, instancing and unsupported UV channels. Geometry, texture and animation budgets also apply. Keep complex source work in Blender or another authoring tool and retain imported playback when conversion is rejected.</p>
  <p>Saved-state export <code>editable-scene</code> requires <code>pageIndex</code> and the imported model's <code>nodeId</code>. It returns canonical document JSON without saving it; review and write it through the existing revision-checked document API. This JSON keeps project asset references, including the source GLB, rather than packaging portable asset bytes. CLI: <code>dsa projects export PROJECT --format editable-scene --node NODE --page 0 --revision N --output editable.json</code>. Assets <strong>Replace uses</strong> and the shared <code>replace-asset</code> operation update references to an existing asset of the same media kind while retaining placements/timing and both library assets; check clip names and duration afterward.</p>
  <h2>Materials</h2><p>Select a 3D object to open <strong>Material</strong> in the inspector. The values persist on the object as <code>scene.material</code>, so the viewport, the raster exports and GLB read the same record.</p>
  <ul><li><strong>Presets</strong> are starting points: Matte (metalness 0, roughness 0.85), Ceramic (0, 0.2), Metal (1, 0.25) and Polished (0.9, 0.08).</li><li><strong>Material color</strong> takes a hex value or a theme token such as <code>$accent</code>. <strong>Metalness</strong> and <strong>roughness</strong> run from 0 to 1; the object's own data supplies the starting value, so the built-in 3D template begins at 0.25 and 0.25.</li><li><strong>Wireframe</strong> draws edges instead of surfaces. <strong>Double sided</strong> also renders back faces; keep it for open or imported single-surface meshes, since closed solids do not need it.</li><li>Stored vertex colors multiply the material color, and a painted base-color map replaces it.</li></ul>
  <p>Edit these in the inspector or with <code>update-node</code>. A <code>scene</code> object is replaced rather than merged, so read it first and send the fields you are not changing back unchanged.</p>
  <p><strong>Emissive color</strong> and <strong>Emissive intensity</strong> control the material's glow contribution. Select owned images for normal, roughness, metalness, emissive and occlusion maps alongside the color texture. These are material maps; bloom remains a page rendering setting.</p>
  <h2>Textures and UV paint</h2><p>Textures are owned project assets. Importing a PNG, JPEG or WebP in <strong>Color texture</strong> uploads it to the project and references it as <code>scene.material.textureAssetId</code>; previews and exports read that asset. An imported model that still carries its own texture offers its original model texture, and external media must be imported before cloud rendering.</p>
  <ol className="docs-numbered"><li><strong>Unwrap.</strong> Add seam edges and unwrap the islands, or use a mesh that already has UVs. Painting is rejected without them.</li><li><strong>Layer.</strong> Create a layer with a name and one map type — <code>color</code>, <code>normal</code> or <code>roughness</code> — plus opacity and visibility. A layer keeps its map type; edit the name, opacity or visibility instead. Up to eight layers persist per object.</li><li><strong>Paint.</strong> A stroke stores a UV point, a radius and a hex color, and a layer keeps at most 256. Strokes stay vector data in the document and are rasterized into the maps whenever the scene is built or exported.</li><li><strong>Resolution.</strong> Choose 256, 512, 1024 or 2048px. One resolution applies to every map on that object.</li><li><strong>Clear before changing UVs.</strong> Strokes are stored in UV space, so unwrapping refuses to run while any layer still holds strokes; clear the layers you no longer need first.</li></ol>
  <p>The UV view draws the islands with an optional distortion tint. Red marks area stretch; it is not proof that islands do not overlap. A stroke sent without a layer writes to the legacy single color map, which has the same 256-stroke budget. GLB/glTF embeds the rasterized maps.</p>
  <h2>Lighting, camera and effects</h2><p>Deselect every object to edit the page's <strong>Camera</strong> and <strong>Lighting</strong> in <code>page.scene</code>. Change them in the inspector or with <code>update-page</code>, preserving the nested fields already present.</p>
  <ul><li><strong>Camera:</strong> position, target, field of view from 10° to 120°, and <code>safeFrame</code> margin from 0 to 0.3. Fit animated subject samples selected subjects' posed bounds across the timeline. Portrait/square shot duplication creates a page with its own camera and shared asset files. Check intermediate wing/tail extremes and overlays; sampled fitting is not a continuous clipping guarantee.</li><li><strong>Lighting:</strong> retain ambient and the original directional light, and add up to eight point, spot or directional lights. Per-light shadow controls and page rendering settings persist with the scene.</li><li><strong>Effects:</strong> fog, exposure, generated environment lighting, shadows, bloom and up to eight seeded particle emitters share the editor, published viewer and raster/video renderer. Emitters have explicit position, spread, velocity, lifetime, count and active interval.</li></ul>
  <p>GLB/glTF carries supported geometry, PBR materials, embedded maps, skins and sampled animations; punctual lights use <code>KHR_lights_punctual</code>. Ambient/environment lighting, fog, bloom, exposure, cast-shadow settings and timed particle behavior are Studio rendering settings rather than a portable glTF effect stack. Review the file under the destination viewer's lighting.</p>
  <h2>Timeline sound</h2><p>Insert an owned audio asset, then use waveform, trim, gain, loop, mute and event controls. Audio/video node <code>data</code> stores <code>audioStart</code>, <code>audioEnd</code>, <code>audioOffset</code> in seconds, <code>audioGain</code> from 0 to 4, <code>audioMuted</code>, <code>audioLoop</code> and <code>audioEvent</code>. Events are labels, not synthesized sounds. Editor/viewer playback and browser/cloud motion exports share cue timing and mixing. Enable audio when browser autoplay is blocked and listen to the actual exported WebM/MP4. Still images and GLB do not carry a soundtrack.</p>
  <h2>Agent workflow</h2><p>Discover <code>sceneCommands</code> through <code>studio_capabilities</code> or <code>/api/schema</code>. WebMCP <code>studio_scene_command</code> accepts a page ID and command, previews by default, and applies with <code>preview:false</code>. <code>studio_inspect_scene</code> returns compact diagnostics at a requested time without transferring vertex buffers.</p>
  <p>Saved-state equivalents are REST GET/POST <code>/api/projects/:id/scene</code>, MCP <code>inspect_scene</code>/<code>author_scene</code>, and CLI <code>dsa scene inspect</code>/<code>dsa scene command</code>. Writes require the observed revision. CLI commands are available in the current source build; an older release archive may not include them.</p>
  <p>WebMCP <code>studio_imported_model</code> accepts <code>pageId</code> and <code>nodeId</code> for inventory; add <code>convert:true</code> to preview conversion and <code>preview:false</code> to apply. <code>studio_frame_scene_shot</code> takes <code>pageId</code>, <code>nodeIds</code>, optional <code>samples</code> (2–61, default 17) and <code>aspect</code> (portrait/square); it previews by default and applies with <code>preview:false</code>. These are open-editor tools. Network clients use existing document/operations and <code>editable-scene</code> export; discover exact schemas at <a href="/api/schema">/api/schema</a>.</p>
  <h2>What the tools check</h2><p>Reports flag degenerate triangles, boundary or non-manifold edges, inconsistent winding, unnormalized skin weights and severe edge stretching in sampled poses. They do not certify self-intersection freedom or animation quality. UV seams can intentionally split vertices.</p><p>Remeshing uses bounded surface sampling rather than semantic quad retopology. Quadruped landmarks and nearest-segment weights are editable starting points. One-shot IK uses mesh-local targets. Persistent two-bone contacts use world targets, bend poles, horizontal ground height, time ranges and rotation limits; inspect measured error for unreachable targets. Explicit UV islands use planar projection and need a distortion/overlap check. Color paint replaces the base-color map. Sculpt, edge splits and plane-cut loops preserve vertex attributes; legacy remesh/relax reject bound geometry. Save a mesh checkpoint before refinement.</p>
  <h2>Shared rigs, clips and repair</h2><p>Share skeleton with attachments explicitly converts compatible legacy copies to one rig reference. Named clip controls adjust speed, amplitude, loop count and blend without rewriting source keys. Later tracks blend over earlier tracks; outside clip intervals the stored pose applies. Joint gizmos edit rest or pose. <code>joint-limits</code> saves per-axis named-bone rotation bounds and optional mirror partner; <code>mirror-pose</code> reflects onto that partner. FK and sampled animation respect those limits. Weight brushes support mirrored stamps, locked joints/vertices and heatmaps.</p>
  <p>Complete-animation diagnostics return issue IDs, affected vertices/faces and times, plus contact drift and floor penetration. Click a warning to select its region and pose; compare the next scan with the previous count. Informational coincident seam boundaries are distinguished from defects and appear only in the first scan frame; warning locations remain time-indexed.</p>
  <h2>Review angles across time</h2><p>Export <code>scene-angles</code> with <code>start</code> alone for front/right/back/left PNGs and <code>views.json</code>. Add <code>end</code> greater than start and <code>reviewSamples</code> (2–25, default 5) for four actual views at each sampled time, <code>contact-sheet.png</code> and camera/node/time diagnostics, including posed bounds, clipping, geometry counts and measured capture costs. Images and the sheet together must fit 64 megapixels. Clipping measurements do not establish absence of occlusion; view the actual images.</p>
  <p>REST export and MCP <code>export_project</code> use <code>start</code>, <code>end</code>, <code>reviewSamples</code>, <code>pageIndex</code> and optional <code>expectedRevision</code>. CLI: <code>dsa projects export PROJECT --format scene-angles --start 0 --end 4 --review-samples 5 --page 0 --revision N --output review.zip</code>. This count differs from scan/framing <code>samples</code> and frame-archive <code>fps</code>.</p>
  <h2>Recover long saves and exports</h2><p>Use durable operations with a stable ID: POST <code>/api/projects/:id/operations</code>, poll GET <code>/api/projects/:id/operations/:operationId</code>, then download <code>/result</code>. An uncertain response must reuse the same ID and payload. Jobs retain owner authorization and observed revisions; export pins the accepted document snapshot.</p><p>WebMCP <code>studio_start_save</code> returns immediately; poll the operation then use <code>studio_reconcile_save</code>. Network MCP exposes <code>start_operation</code>, <code>get_operation</code> and <code>get_operation_result</code>; CLI exposes <code>dsa operations start/status/result</code>. The editor Operations control recalls the latest ID. Save receipts and export artifacts remain private until project deletion. Live collaborative merging retains its separate merge contract.</p>
</>;}
